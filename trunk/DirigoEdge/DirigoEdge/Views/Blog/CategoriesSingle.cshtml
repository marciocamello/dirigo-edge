@{
    ViewBag.Title = "Category";
}
@using DirigoEdge.Utils
@model DirigoEdge.Models.ViewModels.CategorySingleViewModel


<div class="row module blogCatListing">
    <div class="eight columns">
        <div class="stripe darkBlue"></div>
        <h2 class="title sansserif light">@Model.TheCategory.CategoryName</h2>

        @{
            foreach (var blog in Model.BlogRoll)
            {
                <article>
                    <h3><a href="/blog/@ContentUtils.GetFormattedUrl(blog.Title)">@blog.Title</a></h3>
                    <h6>Written by <a href="@blog.BlogAuthorUrl">@blog.Author</a> on @blog.Date.ToLongDateString()</h6>

                    <div class="row">
                        <div class="userBlogContent twelve columns">
                            @Html.Raw(blog.HtmlContent)
                        </div>
                    </div>
                </article>
            }
        }
    </div>

    <div class="four columns categoryBlogListModule">
        @*@Html.Partial("Partials/Blog/BlogCategoryListingPartial")*@
    </div>

</div>